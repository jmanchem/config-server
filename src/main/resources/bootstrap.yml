server:
  port: 8090
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jmanchem/bootcamp-everis-final
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          cloneOnStart: true